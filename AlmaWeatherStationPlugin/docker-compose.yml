version: "2"

services:
    alma-weather-plugin:
        image: alma-weather-plugin:latest
        command: java -jar AlmaWeatherStationPlugin.jar
        links:
            - "kafka"
    kafka:
        image: spotify/kafka:latest
        ports:
            - "9092:9092"
            - "2181:2181"
